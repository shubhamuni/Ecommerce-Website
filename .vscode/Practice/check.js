/*
const marksHeight = 1.69;
const marksWeight = 78;
const jonasHeight = 1.95;
const jonasWeight = 92;

marksBMI = marksWeight/(marksHeight **2);
jonasBMI = jonasWeight/(jonasHeight **2);
console.log(marksBMI, jonasBMI);

if (marksBMI > jonasBMI){
    console.log("Mark's BMI is higher than Jona's BMI");
} 
else {
    console.log("Jona's BMI is higher than Mark's BMI")
    }
if (marksBMI > jonasBMI) {
    console.log(`Marks Bmi (${marksBMI}) is higher than Jonas (${jonasBMI})`)
}
else {
    console.log(`Jonas Bmi (${jonasBMI}) is higher than Marks (${marksBMI})`)
}

let person = prompt("Please enter your name", "Harry Potter");
console.log(person);
*/

