const WelcomePage = () => {
  //It renders only when the user isn't logged in
  return (
    <h1 style={{ textAlign: "center", padding: "15rem", color: "gray" }}>
      Welcome to my page
    </h1>
  );
};

export default WelcomePage;
